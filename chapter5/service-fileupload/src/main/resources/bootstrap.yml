eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8767

spring:
  application:
    name: service-fileupload

  http:
    multipart:
      max-file-size: 300Mb
      max-request-size: 300Mb